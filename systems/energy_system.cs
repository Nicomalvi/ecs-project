public static class EnergySystem
{
    public static void Run(World w)
    {
        for (int i = 0; i < w.energy.dense.Count; i++)
        {
            var energy = w.energy.dense[i];
        }
    }
}
